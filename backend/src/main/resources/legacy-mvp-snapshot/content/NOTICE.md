# 内容状态：已由站点所有者确认（#6 于 2026-08-15 确认）

- `introduction.json`：邮箱 `houxc2249@gmail.com`、displayName `hxc236`、
  headline「构建可靠的全栈应用」已由站点所有者确认；技能分组如需调整请直接修改。
- `projects.json`：三个作品已由站点所有者确认，仓库外链均已验证存在
  （hxc236/myblog、hxc236/jobhunt-ai-helper、hxc236/personal-workbench）。
- `posts.json` 与 `posts/*.md`：当前单篇文章为站点所有者确认的 MVP 架构说明。

后续新增/修改内容时保持字段结构不变；缺少必填字段或外链不可验证会导致构建失败
（这是规格要求的行为：无效必填内容使测试与构建失败，不在运行时静默忽略）。
