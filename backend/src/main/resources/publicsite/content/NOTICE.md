# 内容状态：草稿，等待站点所有者确认（对应 #6）

本目录下的所有内容均为**结构完整的草稿**，用于让 API、页面与测试先跑通。

**在上线前（#6 确认隐私安全的首发内容包）必须由站点所有者逐项确认或替换：**

- `introduction.json`：`email` 是占位值 `TODO-CONFIRM@example.invalid`，必须替换为已验证邮箱；
  `displayName`、`headline`、`introduction`、技能分组需按真实情况修改。
- `projects.json`：三个作品基于本仓库真实提交整理，但标题、摘要、职责、年份与外链需确认；
  每个作品至少保留一个已验证外部目标（仓库或演示）。
- `posts.json` 与 `posts/*.md`：当前文章是架构说明草稿，需替换为站点所有者真实写作；
  **不得虚构文章填充数量**——若确认后只有一篇，列表与详情照常上线。

替换内容时保持字段结构不变；缺少必填字段会导致构建失败（这是规格要求的行为）。
