# 正式 API 使用无版本的领域语义路径

第二阶段的正式 API 使用 `/api/site/...`、`/api/posts/...`、`/api/categories`、`/api/tags` 与 `/api/admin/...` 等领域语义路径，不引入 `/api/v2` 或其他 URL 版本号。现有 `/api/v1` 仅在数据库切换稳定期保留为 MVP 文件读路径的短期回退；未来若需要不兼容变更，必须单独评审并新增 ADR 后再选择版本策略，以免迁移实现细节成为长期公开契约。
